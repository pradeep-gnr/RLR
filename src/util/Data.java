package util;

import java.util.ArrayList;

public class Data {
	/*
	 * A simple wrapper class to represent training data.
	 * Each instance in the data is represented as a training vector and its corresponding class label.   
	 */
		
	/*
	 * An array of data instances. Each Instances consists of a feature vector and a corresponding class label.
	 */
	public ArrayList<Instance> dataInstances ;	
	
	public Data(Matrix matrix)
	{
		/*
		 *  Build a Data instance from a matrix.
		 */	
	}	

}
